@{
    ViewData["Title"] = "Home Page";
}

<h1>Test</h1>

<p id="text">Click the button!</p>

<button id="button">Poke</button>

@section scripts {
     <script>
        $(function () {
            var test = $.connection.test;

            test.client.test = function (message) {
                $("#text").text(message);
            };
                        
            $("#button").click(function() {
                $.ajax({
                    url: "/Test/Poke",
                    method: "GET"
                });
            });

            $.connection.hub.start(function () {
                
            });
        });
    </script>    
}